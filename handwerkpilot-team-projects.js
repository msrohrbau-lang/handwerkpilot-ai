(()=>{
const E=s=>typeof esc==='function'?esc(s):String(s??'');
let hpProjects=[],hpMembers=[],hpAssignments=[];
const manager=()=>['owner','admin','manager'].includes(String(profile?.role||''));
const employee=()=>String(profile?.role||'')==='employee';
const nameOf=id=>hpMembers.find(m=>String(m.user_id)===String(id))?.full_name||hpMembers.find(m=>String(m.user_id)===String(id))?.email||'Mitarbeiter';
async function loadTeam(){const [{data:p},{data:m},{data:a}]=await Promise.all([sb.from('projects').select('*').order('created_at',{ascending:false}),sb.rpc('hp_team_members'),sb.from('project_members').select('*')]);hpProjects=p||[];hpMembers=m||[];hpAssignments=a||[]}
window.hpLoadTeam=loadTeam;
function assigned(projectId){return hpAssignments.filter(a=>String(a.project_id)===String(projectId)).map(a=>String(a.user_id))}
function projectCard(p){const names=assigned(p.id).map(nameOf).join(', ');return `<div class="item"><div class="row"><div><strong>${E(p.name)}</strong><div class="tiny">${E(p.site||'')}</div></div><span class="pill">${E(p.status||'active')}</span></div>${manager()?`<div class="tiny" style="margin-top:8px">👷 ${E(names||'Noch niemand zugewiesen')}</div>`:''}<div class="actions">${manager()?`<button class="btn secondary" onclick="hpEditProject('${E(p.id)}')">Bearbeiten</button>`:''}<button class="btn primary" onclick="hpNewReportForProject('${E(p.id)}')">+ Bautagesbericht</button></div></div>`}
window.hpRenderProjects=()=>{const box=$('hpProjectList');if(box)box.innerHTML=hpProjects.length?hpProjects.map(projectCard).join(''):'<div class="status">Dir ist noch keine Baustelle zugewiesen.</div>'};
function customerOpts(selected=''){return '<option value="">– kein Kunde –</option>'+customers.map(c=>`<option value="${E(c.id)}" ${String(c.id)===String(selected)?'selected':''}>${E(c.name)}</option>`).join('')}
function memberChecks(selected=[]){return hpMembers.map(m=>`<label class="item" style="display:flex;gap:10px;align-items:center"><input type="checkbox" class="hpProjectMember" value="${E(m.user_id)}" ${selected.includes(String(m.user_id))?'checked':''} style="width:22px;height:22px"><div><strong>${E(m.full_name||m.email)}</strong><div class="tiny">${E(m.role||'employee')}</div></div></label>`).join('')||'<div class="status">Noch keine Mitarbeiter im Betrieb.</div>'}
window.hpEditProject=function(id=''){if(!manager())return;const p=id?hpProjects.find(x=>String(x.id)===String(id)):null,sel=p?assigned(p.id):[];$('modalTitle').textContent=p?'Baustelle bearbeiten':'Neue Baustelle';$('modalBody').innerHTML=`<label>Projekt / Baustelle</label><input id="hpPrName" value="${E(p?.name||'')}" placeholder="z. B. Equinix FR6"><label>Ort / Baustelle</label><input id="hpPrSite" value="${E(p?.site||'')}" placeholder="Adresse oder Bezeichnung"><label>Kunde</label><select id="hpPrCustomer">${customerOpts(p?.customer_id||'')}</select><h3>Mitarbeiter zuweisen</h3><div class="list">${memberChecks(sel)}</div><div class="actions"><button class="btn primary" onclick="hpSaveProject('${E(id)}')">Speichern</button></div><div id="hpPrStatus" class="status"></div>`;$('modal').classList.remove('hidden')};
window.hpSaveProject=async function(id=''){const s=$('hpPrStatus'),row={organization_id:profile.organization_id,customer_id:val('hpPrCustomer')||null,name:val('hpPrName'),site:val('hpPrSite'),created_by:session.user.id,updated_at:new Date().toISOString()};if(!row.name){s.textContent='Projektname fehlt.';return}s.textContent='Wird gespeichert …';let r=id?await sb.from('projects').update(row).eq('id',id).select().single():await sb.from('projects').insert(row).select().single();if(r.error){s.textContent=r.error.message;return}const pid=r.data.id,ids=[...document.querySelectorAll('.hpProjectMember:checked')].map(x=>x.value);await sb.from('project_members').delete().eq('project_id',pid);if(ids.length)await sb.from('project_members').insert(ids.map(uid=>({project_id:pid,user_id:uid,organization_id:profile.organization_id})));await loadTeam();hpRenderProjects();s.textContent='✓ Gespeichert';setTimeout(()=>closeModal(),350)};
async function makeInvite(){const {data,error}=await sb.rpc('hp_create_team_invite',{invite_email:'',invite_role:'employee'});if(error)throw error;const token=data?.[0]?.token;if(!token)throw new Error('Einladung konnte nicht erstellt werden.');return `${location.origin}/register-v2.html?invite=${encodeURIComponent(token)}`}
window.hpInviteEmployee=async function(){if(!manager())return;try{const link=await makeInvite(),text=`Hallo, ich lade dich zu HandwerkPilot ein. Über diesen Link kannst du deinen Mitarbeiter-Zugang einrichten:\n${link}`;$('modalTitle').textContent='Mitarbeiter einladen';$('modalBody').innerHTML=`<div class="status">Keine E-Mail-Adresse nötig. Schicke dem Mitarbeiter einfach seinen persönlichen Einladungslink.</div><div class="actions" style="display:grid;gap:10px"><button class="btn primary" id="hpInviteWhatsApp">💬 Über WhatsApp senden</button><button class="btn secondary" id="hpInviteShare">↗️ Link teilen</button><button class="btn secondary" id="hpInviteCopy">🔗 Link kopieren</button></div>`;$('modal').classList.remove('hidden');$('hpInviteWhatsApp').onclick=()=>{location.href=`https://wa.me/?text=${encodeURIComponent(text)}`};$('hpInviteShare').onclick=async()=>{try{if(navigator.share)await navigator.share({title:'HandwerkPilot Einladung',text});else throw 0}catch{await navigator.clipboard?.writeText(link);alert('Einladungslink kopiert.')}};$('hpInviteCopy').onclick=async()=>{await navigator.clipboard?.writeText(link);alert('Einladungslink kopiert.')}}catch(e){alert(e.message)}};
window.hpNewReportForProject=function(id){const p=hpProjects.find(x=>String(x.id)===String(id));if(!p)return;if(typeof openBautagesbericht!=='function')return alert('Bautagesbericht ist noch nicht bereit.');openBautagesbericht();setTimeout(()=>{const pr=$('btProject'),site=$('btSite'),cust=$('btCustomer');if(pr)pr.value=String(p.id);if(site&&!site.value)site.value=p.site||p.name||'';if(cust&&p.customer_id)cust.value=String(p.customer_id)},0)};
function installProjectField(){const old=window.openBautagesbericht;if(!old||old._hpProject)return;function f(id=''){old(id);setTimeout(()=>{if($('btProject'))return;const d=id?documents.find(x=>String(x.id)===String(id)):null,p=d?.payload||{},cust=$('btCustomer');if(!cust)return;const wrap=document.createElement('div');wrap.innerHTML=`<label>Baustelle / Projekt</label><select id="btProject"><option value="">– Projekt wählen –</option>${hpProjects.map(x=>`<option value="${E(x.id)}" ${String(p.project_id||'')===String(x.id)?'selected':''}>${E(x.name)}</option>`).join('')}</select>`;cust.closest('div')?.insertAdjacentElement('afterend',wrap)},0)}f._hpProject=1;window.openBautagesbericht=f}
function lockEmployeeUI(){if(!employee())return;const hideSelectors=['#home .hero','#home .kpis','#home .grid','.bottom button'];document.querySelectorAll('#home .grid .tile').forEach(x=>x.style.display='none');document.querySelectorAll('.bottom button').forEach(x=>x.style.display='none');const bottom=document.querySelector('.bottom');if(bottom){bottom.style.gridTemplateColumns='repeat(2,1fr)';const home=document.createElement('button');home.innerHTML='<span>🏠</span>Start';home.onclick=()=>showView('home');const sites=document.createElement('button');sites.innerHTML='<span>🏗️</span>Baustellen';sites.onclick=()=>showView('projects');bottom.append(home,sites)}const grid=document.querySelector('#home .grid');if(grid){grid.style.gridTemplateColumns='1fr';grid.innerHTML='';const b=document.createElement('button');b.className='tile';b.onclick=()=>showView('projects');b.innerHTML='🏗️ Meine Baustellen<small>Zugewiesene Baustellen öffnen und Bautagesbericht erstellen.</small>';grid.appendChild(b)}const hero=document.querySelector('#home .hero');if(hero)hero.innerHTML='<div class="pill">Mitarbeiter</div><h1>Meine Baustellen</h1><p>Hier siehst du nur deine zugewiesenen Baustellen und kannst Bautagesberichte erfassen.</p>';document.querySelectorAll('#home .card').forEach(c=>{if(c.querySelector('.kpis')||c.querySelector('#aiInput'))c.style.display='none'});['crm','docs','wiso','accounting'].forEach(id=>{const v=$(id);if(v)v.style.display='none'})}
function installUI(){const app=document.querySelector('.app'),wiso=$('wiso');if(!app||!wiso)return;let sec=$('projects');if(!sec){sec=document.createElement('section');sec.id='projects';sec.className='view hidden';sec.innerHTML=`<div class="row"><div><h2 style="margin:0">${employee()?'Meine Baustellen':'Baustellen & Team'}</h2><div class="tiny">${employee()?'Zugewiesene Baustellen und Bautagesberichte':'Mitarbeiter zuweisen und Bautagesberichte einsammeln'}</div></div>${manager()?'<button class="btn primary" onclick="hpEditProject()">+ Baustelle</button>':''}</div>${manager()?'<div class="actions"><button class="btn secondary" onclick="hpInviteEmployee()">👤 Mitarbeiter einladen</button></div>':''}<div id="hpProjectList" class="list" style="margin-top:12px"></div>`;app.insertBefore(sec,wiso)}if(manager()){const grid=document.querySelector('#home .grid');if(grid&&!grid.querySelector('[data-hp-projects]')){const b=document.createElement('button');b.className='tile';b.dataset.hpProjects='1';b.setAttribute('onclick',"showView('projects')");b.innerHTML='🏗️ Baustellen & Team<small>Mitarbeiter zuweisen und Berichte einsammeln.</small>';grid.appendChild(b)}}const oldShow=window.showView;if(oldShow&&!oldShow._hpProjects){const sh=function(id){if(employee()&&!['home','projects','bautage'].includes(id))id='home';oldShow(id);if(id==='projects')loadTeam().then(hpRenderProjects)};sh._hpProjects=1;window.showView=sh}lockEmployeeUI()}
setTimeout(async()=>{await loadTeam();installProjectField();installUI();hpRenderProjects()},700);
})();